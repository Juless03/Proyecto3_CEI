let
  type IntList ~ pointer Node;
  type Node ~ record
      head: Integer
      tail: IntList
    end;
  var list: IntList
in
  begin
    list:=new(Node);
    list.>.tail:=new(Node);

  end
