let
  type IntList ~ pointer Node;
  type Node ~ record
      head: Integer
      tail: Integer
    end;
  var list: IntList


in
  begin
    list:=new(Node);
    list.head:=5;
    putint(list.head)

  end
