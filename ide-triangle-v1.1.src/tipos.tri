let
  type IntList ~ pointer Node;
  type Node ~ record
      head: IntList
      tail:Integer
    end;
  var list: IntList
in
  begin
    list:=new(Node);
    list.>.head:=new(Node);
  end
